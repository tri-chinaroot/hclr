# HCLR 局限性（LIMITATIONS）

> 使用前必读。HCLR 是一个轻量自我评估方法，不是科学量表，也不是绩效裁判。

## 1. 核心边界

- **不是客观真理标准**：采纳和认可不等于成果客观正确；
- **不是一般智力/认知水平量表**：HCLR 只反映“AIGC 使用情境下的稀疏介入能力”参考；
- **不是 AI 使用能力的唯一评价方法**；
- **不用于招聘、淘汰、绩效排名等高风险裁决**；
- **不用于跨人绝对排名**（除严格受控的 Challenge 场景）。

## 2. 测量学局限

### 2.1 P1–P5 未标定

P1–P5 是顺序等级，不是等距量表。参考值中的分层分是启发式映射，不是测量学结论。在完成配对比较、锚定案例或 IRT 标定前，P 只能按分类变量解读。

### 2.2 分母不测量认知投入

介入量（判断数/字数/次数）只测量显性介入形式，不包含形成判断所需的审阅、搜索、核查、推理成本。HCLR 当前衡量的是“显性介入杠杆”，不是完整认知成本。

### 2.3 共同方法偏差

所有评分（P、C1、C2）都由同一使用者完成。第二轮确认是使用者对真实反馈的解读，不是受众直接评分。这可能导致自利归因、认知失调和一致性动机。

### 2.4 选择偏差

- 成功、重要、印象深刻的成果更容易被补录第二轮；
- 未采用任务可能不被记录；
- 被放弃的任务可能不进入统计；
- 长反馈周期任务会被系统性低估。

因此第二轮相关指标是“已采用、已交付且已反馈子样本中的条件比率”，不代表整体表现。

### 2.5 无法完全归因

从 O0 到 O1 的改变不必然全部由人类介入造成。生成模型具有随机性，重新生成、上下文积累也可能产生变化。当前方法未内置反事实对照。

### 2.6 可博弈性

使用者可能通过以下方式虚高分数：

- 选择表现差的模型或容易改写大的任务；
- 压缩表达减少介入量；
- 只记录成功案例；
- 自行高评 P；
- 不补录失败结果。

## 3. 解释限制

- 曲线上升**不能**直接解释为认知水平提升，只能作为稀疏介入能力变化的参考；
- 曲线下降可能来自任务变难、模型变化、受众变化，不能直接解释为能力退步；
- 任务数过少时（<10）趋势无统计意义；
- 换模型、换领域、换受众必须标记断点。

## 4. 隐私与数据

- 记录内容可能包含商业机密、个人信息、客户数据；
- 谁有权访问、如何保存、是否可删除，必须在使用前明确；
- 详见 [PRIVACY.md](PRIVACY.md)。

## 5. 结论

HCLR 的价值在于帮助使用者**观察自己与 AI 的协作模式**，而不是给别人打分。请把指标当作镜子，而不是尺子。
