# HCLR 方法说明（白皮书）

> 版本：0.1 草案  
> 定位：面向 AI 使用者的稀疏介入能力自我评估方法

## 1. 背景问题

大模型大幅降低了生成成本，但“生成多”不等于“做得好”。在真实工作中，AI 产出能否成为可采纳的成果，仍高度依赖使用者是否在关键节点提供了正确的人类判断。

现有工具大多只记录“是否满意”（点赞/点踩），无法回答：

- 使用者是否真正改变了 AI 的输出？
- 改变发生在哪个层面——措辞、局部内容、结论、整体方案，还是问题框架？
- 使用者投入了多少关键判断，才让成果达到可采纳？
- 一段时间后，使用者是否越来越能以更少的介入产生更大的有效改变？

HCLR（Human Cognitive Leverage Ratio，人类认知杠杆率）尝试为这些问题提供一种轻量、可持续的记录与观察方法。

## 2. 基本定义

**HCLR 是一种面向 AI 使用者的稀疏介入能力自我评估方法。** 在给定 AIGC 模型、任务领域和观察周期内，使用者记录自己通过有限的显性介入使生成成果发生的改变，并进行两轮确认：生成阶段确认介入后的成果是否达到本人愿意采纳的标准；成果触达受众后，根据真实反馈确认成果是否获得受众认可。两轮确认、成果改变范围与介入量共同形成 HCLR 参考值。

形式化条件表示：

```text
HCLR(u | m, d, T)
```

其中：

- `u`：使用者
- `m`：模型及版本（相对稳定的工具环境）
- `d`：任务领域
- `T`：观察周期

**模型视为既定工具环境**。HCLR 不要求完全剥离人与模型的贡献，但更换模型、任务领域或受众时应标记序列断点，避免混比。

## 3. 基本记录单元

### 3.1 任务事件

基本计算单位为**任务—成果转换事件**（简称“任务事件”）。一次任务事件指：

> 一个明确的 AI 协作任务，从原始生成开始，经过若干人类介入，形成可确认的最终成果。

单条介入不单独作为 HCLR 计算单位，只作为事件内部的追踪记录。这避免了把同一成果改变重复归因到多条介入。

### 3.2 记录字段

每个任务事件记录：

| 字段 | 说明 | 必填 |
|---|---|---|
| `task_id` | 任务标识 | 是 |
| `domain` | 任务领域 | 是 |
| `model` | 模型及版本 | 是 |
| `audience` | 目标受众类型 | 是 |
| `O0` | 原始生成（冻结，不可覆盖） | 是 |
| `O1` | 介入后最终成果 | 是 |
| `interventions[]` | 介入列表（时间、文本、类型） | 是（可为空） |
| `I` | 介入量（口径见 SCORING.md） | 是 |
| `P` | 改变范围等级 P1–P5 | 是 |
| `C1` | 第一轮确认：0 未采用 / 1 已采用 | 是 |
| `C2` | 第二轮确认：1 认可 / 0 未认可 / ∅ 待确认 | 否（触达受众后） |
| `C2_note` | 第二轮确认依据（真实反馈描述） | 否 |
| `created_at` / `confirmed_at` / `c2_at` | 时间戳 | 是/否/否 |

## 4. 人类介入

### 4.1 什么是介入

介入是使用者**主动、自主**发出的关键判断，例如：

- 事实纠正；
- 目标或范围调整；
- 约束补充；
- 结论修正；
- 分析框架改变；
- 风格与受众适配。

### 4.2 介入必须主动

人类介入必须是主动、自主的。AI 不得通过主动学习（Active Learning）机制决定人的审查配额、介入对象或介入边界。HCLR 测量的是人类自主发现并压缩问题为关键约束的能力，而不是被 AI 点名提问后的配合能力。

### 4.3 稀疏是事后观察属性

“稀疏”不是 AI 预先分配的反馈预算，而是事后观察到的属性：介入少但改变大。稀疏与否通过结果判断，不由系统预先设定。

## 5. 双重确认

### 5.1 第一轮确认 C1

生成和介入完成后，使用者确认本人是否采纳：

```text
C1 = 1  → 采纳（介入后成果达到本人愿意采纳的标准）
C1 = 0  → 不采纳（成果未达到标准，或任务放弃）
```

### 5.2 第二轮确认 C2

成果触达目标受众后，使用者根据真实反馈确认受众是否认可：

```text
C2 = 1   → 根据真实反馈，受众认可
C2 = 0   → 根据真实反馈，受众未认可
C2 = ∅   → 尚未获得足够反馈（待确认）
```

第二轮是**使用者对真实反馈的解读**，不是生成阶段对受众认可的预测，也不要求研究者直接调查受众。

### 5.3 待确认不等于失败

- 未获得足够反馈的任务记为“待确认”，不得按 0 处理；
- 延迟获得的第二轮结果回填至**原始任务批次**，不计入收到反馈时的新周期；
- 报告时应同时披露待确认比例。

### 5.4 结果状态

| 状态 | C1 | C2 | 含义 |
|---|---|---|---|
| S0 | 0 | — | 未采用 |
| S1 | 1 | ∅ | 已采用，待确认 |
| S2 | 1 | 0 | 已采用，未获认可 |
| S3 | 1 | 1 | 已采用并获认可 |

## 6. 改变范围等级 P1–P5

| 等级 | 含义 | 示例 |
|---|---|---|
| P1 | 局部措辞、事实或单项内容改变 | 修正错别字、补充一个数据点 |
| P2 | 一个模块、主要段落或一条论证改变 | 重写某段论证逻辑 |
| P3 | 多个主要内容或核心结论改变 | 结论方向调整，多处内容修正 |
| P4 | 整体方案、主要建议或交付结构改变 | 交付结构重排，主要建议更换 |
| P5 | 问题定义、分析框架或最终方向改变 | 重新定义问题，更换分析框架 |

> P1–P5 是顺序等级（ordinal），不是已验证的等距量表。在完成标定前，不直接参与比率计算；详见 SCORING.md 的处理方式。

## 7. 典型使用流程

```text
1. 创建任务事件（记录领域、模型、受众）
2. 冻结 AI 初稿 O0
3. 记录每一次人类介入
4. 生成介入后成果 O1
5. 评估改变范围 P（AI 可建议，人类最终确认）
6. 第一轮确认 C1（采纳/不采纳）
7. 成果交付受众
8. 获得真实反馈后完成第二轮确认 C2
9. 定期生成个人报告与趋势
```

## 8. 重要边界

- HCLR 是**自我评估参考**，不是客观真理标准；
- 不是人的一般智力或总体认知水平量表；
- 不是 AI 使用能力的唯一评价方法；
- 主要用于同一使用者的**纵向自我比较**，不宜作为跨人绝对排名；
- 不宜作为招聘、淘汰、绩效排名等高风险裁决工具；
- HCLR 变化曲线只能作为使用者稀疏介入能力变化的**参考**，不能直接声称证明认知水平提升。

## 9. 应用层级

1. **个人层**：自我观察、复盘与趋势跟踪；
2. **组织层**：团队协作方式比较（在统一模型、统一任务条件下）；
3. **厂商层**：从重复出现的高杠杆介入中识别模型盲点、评测项与训练材料（第二层应用，详见 PRODUCT_VISION.md）；
4. **社区层**：在统一规则下形成排行榜与挑战（需严格条件控制，见 SCORING.md）。

## 10. 版本

- 0.1：方法草案，含 CLI 原型；
- 后续版本将根据真实使用反馈校准字段与计分规则。
