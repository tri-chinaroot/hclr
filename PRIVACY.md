# HCLR 隐私与数据政策（草案）

> 版本：0.1。本文档描述 HCLR 项目对数据的处理原则。正式开源前将依据具体授权方式细化。

## 1. 数据范围

HCLR 记录可能包含：

- 与 AI 的完整对话与原始输出（O0）；
- 使用者介入内容（可能包含业务判断、客户信息）；
- 介入后成果（O1）；
- 受众反馈描述；
- 使用行为元数据（时间、模型、任务领域）。

## 2. 基本原则

1. **用户数据属于用户**：本项目不会将用户记录用于任何未经授权的用途；
2. **本地优先**：记录默认保存在使用者本地，不上传任何服务器；
3. **最小必要**：仅采集计算 HCLR 所需字段；
4. **可删除**：使用者可随时删除全部或部分记录；
5. **可导出**：记录以开放格式（JSON/CSV）导出；
6. **透明**：任何聚合、匿名化或第三方使用都需明确披露并获得同意。

## 3. 本地存储

CLI 原型将数据保存在本地 SQLite 文件（`~/.hclr/hclr.db`），默认不联网。

## 4. 未来可能的数据使用

| 场景 | 前置条件 |
|---|---|
| 个人报告/趋势 | 无需额外同意（本地计算） |
| 团队诊断 | 团队内部约定 + 脱敏 |
| 模型厂商优化 | 单独签署数据授权协议，明确用途、期限、可撤销性 |
| 排行榜/公开案例 | 明确同意 + 脱敏 + 可选匿名 |

## 5. 安全提示

- 不要在不安全的共享设备上保存含商业机密的记录；
- 导出文件注意访问权限；
- 涉及客户/员工数据的任务建议脱敏后再记录。

## 6. 当前状态

CLI 原型仅本地运行，不包含任何网络上传功能。
