# HCLR 产品愿景（PRODUCT_VISION）

> 本文档描述 HCLR 从个人方法到产品与商业应用的路径。当前阶段聚焦第一层。

## 1. 定位

HCLR 首先是一个**面向 AI 使用者的自我评估方法**。它帮助使用者回答：

- 我的判断对 AI 输出产生了多大影响？
- 我是否越来越能用更少、更关键的介入获得可采纳成果？

## 2. 四层应用

### 第一层：个人反思工具（当前重点）

- 记录任务事件；
- 生成个人趋势报告；
- 提供高杠杆案例回看；
- 帮助使用者改进协作方式。

**成功标准**：使用者愿意持续记录，并认为报告有实际价值。

### 第二层：组织协作诊断

- 团队在统一模型/任务条件下比较协作方式；
- 识别团队级的高杠杆介入模式；
- 用于培训与流程改进（不作为绩效裁决）。

### 第三层：模型厂商反馈基础设施

厂商可以从 HCLR 事件链中获得：

```text
O0 → h → O1 → C1 → C2
```

真正有价值的是：**哪些少量、重复出现的人类判断，能够系统性纠正模型输出**。

可转化为：

- 模型盲点分类；
- 评测集构造；
- 人类反馈训练样本；
- 提示词与产品流程优化；
- 模型版本缺陷对比。

**关键原则**：

- HCLR 评价使用者，不等于模型质量指标；
- 模型厂商应另行观察：直接采用率、介入后采用率、双重确认通过率、达到确认所需介入量；
- 不能把介入后的优秀成果全部归因于模型；
- 即时“好/坏”按钮 ≠ 第一轮采纳确认 ≠ 第二轮受众结果确认，三者必须区分。

**商业前提**：需要单独的数据授权协议，明确用途、期限、可撤销性与贡献归因。

### 第四层：排行榜与社区（HCLR Challenge）

营销价值：让使用者在统一规则下 PK，展示“谁能让 AI 发生更大改变”。

**必要条件**（否则榜单无意义）：

- 统一模型与版本；
- 统一任务集与初始输出；
- 统一介入量口径；
- 第三方/社区盲评改变范围；
- 完整交互记录可审计；
- 必须包含失败任务，禁止选择性记录。

建议与个人 HCLR 分开为两个指标：

- 个人 HCLR：严肃纵向观察；
- Challenge Score：公开竞赛分数。

## 3. 产品路线

### 阶段一：开源方法与本地 MVP（当前）

- 方法文档 + JSON Schema + CLI 原型；
- 验证：用户是否愿意持续记录、字段是否可自动采集、P1–P5 是否易理解、第二轮是否有人补录。

### 阶段二：个人 AI 协作仪表盘

- 多模型接入；
- 自动版本对比；
- 介入类型识别；
- 周报/月报；
- 分领域 HCLR；
- 高杠杆案例库。

### 阶段三：HCLR Challenge

- 统一任务与规则；
- 周挑战、领域榜、团队赛；
- “一句话改变 AI 方案”挑战；
- 高杠杆介入案例展示。

### 阶段四：厂商 SDK 与数据合作

- 事件记录 Schema；
- 双重确认 SDK；
- 延迟反馈机制；
- 高杠杆介入聚类；
- 模型版本对比；
- 隐私保护与授权协议。

## 4. 关键判断

> HCLR 不应先证明自己是“科学量表”，再寻找用户。  
> 更合理的路径：公开方法 → 做出可用工具 → 获得真实交互记录 → 发现用户是否愿意持续使用 → 校准指标 → 形成标准任务与排行榜 → 再讨论正式测量学与厂商合作。

## 5. 开放决策

公开 GitHub 前需要决策：

1. **专利策略**：是否申请专利；公开可能影响新颖性；
2. **开源协议**：方法文档、代码、数据分别采用什么授权（MIT/Apache/CC-BY/保留权利）；
3. **隐私承诺**：确认“本地优先、默认不联网”的边界；
4. **品牌**：HCLR 名称与中文名“人类认知杠杆率”的使用规则。
