# HCLR 计分规则（SCORING）

> 版本：0.1 草案  
> 本文档定义指标、公式与口径。所有数值在当前阶段均为**参考值**，不构成科学量表结论。

## 1. 指标定义

### 1.1 介入量 I

记录使用者投入的显性介入量。支持三种口径（记录时声明采用哪一种）：

| 口径 | 定义 | 优点 | 局限 |
|---|---|---|---|
| 判断数 | 独立语义判断/关键意见条数 | 语义上接近“关键介入” | 依赖拆分方式 |
| 字数 | 介入文本总字符数 | 简单、可自动统计 | 长度≠认知投入 |
| 次数 | 介入操作次数（回合数） | 最简单 | 忽略单次复杂度 |

> 当前阶段推荐**判断数**作为默认口径，但必须在记录中声明。  
> **重要边界**：显性介入量不等于完整认知投入（审阅、搜索、核查、推理的成本未计入）。因此 HCLR 当前衡量的是“显性介入杠杆”，不是完整的认知能力得分。

### 1.2 改变范围 P

见 METHOD.md 第 6 节。P1–P5 为顺序等级。

**当前阶段处理**：P 不直接作为等距数值参与计算，先按分类变量分层报告：

- 各等级任务占比；
- 按 P 分层的采纳率；
- 按 P 分层的第一轮/第二轮结果分布。

**未来标定路径**（完成其中任意一项后方可数值化）：

- 配对比较法由使用者或独立评分者标定 P 等级间距；
- 收集足够样本后用量表模型（如 Rasch/IRT）检验单维性与间距；
- 对锚定案例进行外部盲评一致性检验。

### 1.3 两轮确认

见 METHOD.md 第 5 节。

## 2. 建议辅助指标（不合成总分）

| 指标 | 公式 | 含义 |
|---|---|---|
| 直接采用率 | 无介入即采用的任务 / 全部任务 | 基线：初稿质量 |
| 介入后采用率 | C1=1 的任务 / 有介入任务 | 介入是否带来可采纳成果 |
| 第二轮完成率 | 有 C2 结果的任务 / C1=1 的任务 | 反馈回填完整度 |
| 已确认认可率 | C2=1 的任务 / 有 C2 结果的任务 | 受众认可比例（已确认样本内） |
| 平均介入量 | ΣI / 任务数 | 每任务介入成本 |
| 高杠杆案例 | C1=1 且 C2=1 且 P≥P3 且 I≤中位数 | 少量介入带来高价值改变 |

> 注意区分**第二轮完成率**（分母=全部已采用任务）与**已确认认可率**（分母=已完成第二轮任务）。二者混用是常见错误。

## 3. 参考分值（实验性）

以下分值为**实验性参考**，仅用于个人纵向观察，不用于跨人比较：

```text
任务事件参考值 = P(分层分) / I(介入量)
```

其中 P 的分层分在未标定前仅用于方向性观察，例如：

- P1/P2 → 1；
- P3 → 2；
- P4 → 3；
- P5 → 4。

**此映射是启发式，不是测量学结论。** 在完成标定前，报告中必须同时展示原始 P 分布，避免把参考值误读为精确分数。

## 4. 周期与汇总

- 以自然月为默认观察周期 `T`，可自定义；
- 周期内报告：任务数、各指标、P 分布、C1/C2 状态分布、待确认比例；
- 换模型、换任务领域、换受众时在序列中标记断点；
- 任务数过少（建议 <10）时只展示原始记录，不展示趋势结论。

## 5. 缺失与偏差处理

- 第二轮未反馈 → 记为 S1 待确认，不计 0；
- 延迟反馈 → 回填原任务批次；
- 报告必须披露待确认比例；
- 提供敏感性分析：把全部待确认视为认可（乐观）与视为未认可（保守）的上下界；
- 若待确认比例 >30%，第二轮相关指标标注“不可靠”。

## 6. 排行榜条件（HCLR Challenge，仅用于社区营销场景）

跨人比较**只在严格条件下**允许：

- 统一模型与版本；
- 统一任务集与初始输出；
- 统一介入量口径；
- 改变范围由第三方或社区盲评；
- 完整交互记录可审计；
- 禁止选择性记录（必须包含失败任务）；
- 异常结果复核。

## 7. 记录 Schema

字段定义见 [`../schema/hclr-record.schema.json`](../schema/hclr-record.schema.json)。

## 8. 报告输出

CLI 工具生成的个人报告包含：

- 周期内任务数；
- 直接采用率、介入后采用率；
- 第二轮完成率、已确认认可率；
- P1–P5 分布；
- S0–S3 状态分布；
- 高杠杆案例列表；
- 参考值趋势（注明启发式映射）；
- 断点标注（模型/领域/受众变化）。
