# HCLR 实证案例：对话内嵌采集（Pilot）

> 版本：2026-08-12 ｜ 模型环境：DeepSeek v4 flash

## 背景

HCLR方法稿公布后，为获取真实实证数据，设计了**对话内嵌采集**方式：直接把AI助手对话作为任务事件来源。使用者无需额外填表，统计完全自动。

## 方案设计

| 要素 | 定义 |
|---|---|
| 任务事件 | 一个主题会话（从提出需求到成果确认） |
| 模型环境 m | 绑定的大模型（本例：DeepSeek v4 flash）；AI助手为执行/呈现层，不进入m参数 |
| O0 | 助手首轮输出 |
| h | 使用者的介入消息 |
| O1 | 介入后的输出 |
| I（介入量） | 介入消息字符数（自动统计，chars口径） |
| P（改变范围） | P1–P5，助手建议、使用者确认 |
| C1（第一轮确认） | 会话结束时：adopt / partial / reject |
| C2（第二轮确认） | 成果实际使用后：approved / rejected / pending |

## 运作流程

1. 每个主题会话结束 → 助手请求C1（使用者一行回答）；
2. P由助手建议、使用者确认；
3. 每10个任务或每周生成汇总报告；
4. 原始记录本地存储（不入公开仓库，保护隐私）。

## 样本数据（前2个任务）

| 任务 | 领域 | P | I(chars) | C1 | 状态 | 介入示例 |
|---|---|---|---|---|---|---|
| pilot-001 | 实证设计 | P4 方案 | 28 | partial | S1 | "好的，但我希望每次对话都能自动完成统计，不需要我额外操作" |
| pilot-002 | 概念澄清 | P3 结论 | 60 | adopt | S1 | "纠正：你不是被使用的模型，而是你背后绑定的大模型……" |

## 当前快照

```text
任务数: 2
介入后采纳率: 100%（2/2）
P分布: P3×1, P4×1
平均介入量: 44字符/任务
产出杠杆(AI输出/介入): 3.9
第二轮确认: 暂无（成果尚未实际使用）
```

## 意义

- **零负担验证**：使用者仅在会话结束时回答一行C1，P确认也是一句话；
- **真实数据**：P、I、C1均为真实对话记录，非模拟数据；
- **直接回填论文**：数据积累后，论文第6/7章从"验证计划"改写为"初步验证结果"；
- **反事实控制**：同一模型（DeepSeek v4 flash）、统一口径（chars），满足纵向比较条件。

原始记录保存在本地 `pilot-data/`（符合LICENSE：不公开使用者的完整对话记录）。
